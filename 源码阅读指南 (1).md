# 源码阅读指南

> 适用于：Java / Vue 初学者 · 个人财务管理系统
> 目标：从零看懂整个项目，每一步都知道自己在看什么、为什么这样写

---

## 一、项目全貌：30 秒理解这个系统在做什么

这是一个**前后端分离**的个人财务管理 Web 应用：

```
用户注册/登录 → 记录收入和支出 → 按分类统计 → 3D 小票可视化展示
```

**技术架构一句话**：Vue 3 前端发 HTTP 请求 → Spring Boot 后端处理 → MySQL 存数据 → JWT 做身份认证

---

## 二、阅读前：你需要会什么

### 必须懂（否则看不懂）
| 知识点 | 最低要求 | 不懂怎么办 |
|--------|---------|-----------|
| Java 类和对象 | 知道什么是字段、方法、构造函数 | 专项补 30 分钟就够 |
| List / Map | 知道怎么存取数据 | 专项补 20 分钟 |
| 注解 | 看到 `@` 开头知道这是注解 | 搜索"Spring 注解入门" |
| SQL 基础 | SELECT / INSERT / UPDATE / DELETE | 能看懂就行 |
| HTTP 基础 | GET 和 POST 的区别 | 5 分钟就够 |

### 不需要先懂（项目里会学到）
- Spring Boot 怎么配置 → 看第三章
- MyBatis-Plus 怎么用 → 看第四章
- JWT 是什么 → 看第五章
- Three.js 怎么画 3D → 看第七章

---

## 三、项目结构地图

先看整体目录，建立空间感：

```
my_project/
│
├── finance-ui/                        ← 前端（Vue 3 + Vite）
│   ├── src/
│   │   ├── api/                       ← 【关键】HTTP 请求封装
│   │   │   ├── request.js             ★ 统一 axios 实例 + JWT 注入 + 401 拦截
│   │   │   ├── auth.js                ← 登录注册接口
│   │   │   ├── bill.js                ← 账单 CRUD 接口
│   │   │   ├── category.js            ← 分类查询接口
│   │   │   └── statistics.js          ← 统计数据接口
│   │   │
│   │   ├── components/                ← UI 组件
│   │   │   ├── InteractiveReceipt.vue ★ 3D 小票（Three.js 布料物理）
│   │   │   ├── CoinParticles.vue      ← 硬币粒子动画（Canvas）
│   │   │   ├── BackgroundParticles.vue← 背景粒子
│   │   │   ├── MetricCard.vue         ← 数据卡片（带音效）
│   │   │   ├── BillList.vue           ← 账单列表
│   │   │   ├── ChartPanel.vue         ← ECharts 图表
│   │   │   ├── Sidebar.vue            ← 侧边栏导航
│   │   │   ├── AddTransactionModal.vue← 新增账单弹窗
│   │   │   └── ConfirmModal.vue       ← 确认删除弹窗
│   │   │
│   │   ├── views/                     ← 页面
│   │   │   ├── AuthPage.vue           ← 登录注册页
│   │   │   └── FinanceDashboard.vue   ★ 主仪表盘（核心页面）
│   │   │
│   │   ├── i18n/                      ← 国际化（中/英）
│   │   ├── utils/sounds.js            ← Web Audio 音效合成
│   │   ├── App.vue                    ★ 根组件（路由 + 登录状态）
│   │   ├── main.js                    ← 入口
│   │   ├── style.css                  ← 全局样式
│   │   └── tokens.css                 ← 设计令牌（CSS 变量）
│   │
│   └── vite.config.js                 ★ Vite 配置（API 代理）
│
├── src/main/java/com/example/personalfinance/   ← 后端（Spring Boot）
│   │
│   ├── controller/                    ← 接收 HTTP 请求
│   │   ├── AuthController.java        ★ 登录注册（入口）
│   │   ├── BillController.java        ★ 账单 CRUD（核心业务）
│   │   ├── CategoryController.java    ← 分类管理
│   │   ├── StatisticsController.java  ← 统计查询
│   │   └── AdminUserController.java   ← 管理员接口
│   │
│   ├── service/                       ← 业务逻辑
│   │   ├── impl/
│   │   │   ├── BillServiceImpl.java   ★ 账单业务实现
│   │   │   ├── AppUserServiceImpl.java← 用户业务（注册/登录）
│   │   │   ├── CategoryServiceImpl.java
│   │   │   └── StatisticsServiceImpl.java ★ 统计 SQL（复杂查询）
│   │   ├── IBillService.java          ← 接口定义
│   │   └── ...
│   │
│   ├── mapper/                        ← 数据库操作（MyBatis-Plus）
│   │   ├── BillMapper.java
│   │   ├── CategoryMapper.java
│   │   └── AppUserMapper.java
│   │
│   ├── entity/                        ← 数据库表对应的 Java 类
│   │   ├── Bill.java                  ★ 账单实体
│   │   ├── Category.java              ← 分类实体
│   │   └── AppUser.java               ← 用户实体
│   │
│   ├── dto/                           ← 前端 → 后端 的数据格式
│   │   ├── BillCreateDTO.java         ★ 新增账单的请求体
│   │   ├── BillUpdateDTO.java         ← 编辑账单的请求体
│   │   ├── LoginDTO.java              ← 登录请求体
│   │   └── ...
│   │
│   ├── vo/                            ← 后端 → 前端 的数据格式
│   │   ├── LoginVO.java               ★ 登录响应（含 token）
│   │   ├── DashboardVO.java           ← 仪表盘数据
│   │   └── CategoryStatVO.java        ← 分类统计
│   │
│   ├── interceptor/
│   │   └── AuthInterceptor.java       ★ JWT 认证拦截器（权限核心）
│   │
│   ├── annotation/
│   │   └── RequireRole.java           ★ 自定义角色注解
│   │
│   ├── common/
│   │   ├── ApiResponse.java           ← 统一返回格式
│   │   └── UserContext.java           ★ ThreadLocal 存当前用户
│   │
│   ├── config/
│   │   ├── CorsConfig.java            ← 跨域配置
│   │   ├── WebMvcConfig.java          ★ 拦截器注册
│   │   ├── RedisConfig.java           ← Redis 缓存配置
│   │   └── MyBatisPlusConfig.java     ← 分页插件配置
│   │
│   ├── exception/
│   │   └── GlobalExceptionHandler.java← 全局异常处理
│   │
│   ├── util/
│   │   └── JwtUtil.java               ★ JWT 工具类
│   │
│   └── PersonalFinanceApplication.java← 启动类
│
├── src/main/resources/
│   ├── application.properties         ★ 全部配置（环境变量占位）
│   └── schema.sql                     ← 建表脚本（自动执行）
│
├── sql/init.sql                       ← 手动初始化脚本（含示例数据）
├── pom.xml                            ★ Maven 依赖配置
└── README.md                          ← 项目说明
```

> **带 ★ 的文件是核心文件**，阅读时优先看这些。

---

## 四、推荐阅读路线

### 路线图

```
第1站：数据库 → 第2站：一条完整链路 → 第3站：登录认证
→ 第4站：分层设计 → 第5站：统计查询 → 第6站：前端交互
→ 第7站：3D 可视化 → 第8站：权限系统
```

---

### 第 1 站：看数据库（建立地图）

打开 `sql/init.sql`，做三件事：

1. **列出三张表**：`category`、`bill`、`app_user`
2. **看字段和类型**：注意 `amount` 用 `DECIMAL(10,2)` 不用 `DOUBLE`（为什么？浮点数有精度丢失）
3. **看关联关系**：`bill.category_id` → `category.id`（外键）

```
app_user (1) ──── (N) bill        一个用户有多条账单
app_user (1) ──── (N) category    一个用户有多个私有分类
category  (1) ──── (N) bill       一个分类对应多条账单
```

**关键技术点**：
- `category.user_id = NULL` 表示全局分类（所有人可见），有值表示用户私有分类
- `bill.user_id` 实现数据隔离——每个用户只能看到自己的账单
- `app_user.role` 默认 `USER`，支持 `ADMIN` / `VIEWER` 三种角色

> 这是整个项目的骨架，看懂了后面不会迷路。

---

### 第 2 站：跟一条完整功能链路

从**新增账单**开始，按以下顺序读：

```
① 用户点击"新增"按钮
   ↓  文件：AddTransactionModal.vue
② axios 发出 POST /api/bills 请求
   ↓  文件：api/bill.js → api/request.js（自动加 JWT）
③ BillController.createBill() 接收请求
   ↓  文件：controller/BillController.java
④ 调用 BillService.saveBill()
   ↓  文件：service/impl/BillServiceImpl.java
⑤ 调用 BillMapper.insert()
   ↓  文件：mapper/BillMapper.java（MyBatis-Plus 自动生成 SQL）
⑥ 写入 bill 表
   ↓  数据库
⑦ 返回 ApiResponse<BillVO>
   ↓  前端收到响应，刷新列表
⑧ 触发硬币音效 + 粒子动画
   ↓  文件：utils/sounds.js + CoinParticles.vue
```

**每一步搞清楚**：数据叫什么名字？从哪里传到哪里？格式是什么？

**关键技术点**：
- `request.js` 的请求拦截器自动从 `localStorage` 取 token 加到 `Authorization` 头
- `BillController` 从 `UserContext` 取 `userId`，确保只能操作自己的数据
- `ApiResponse` 统一包装返回格式：`{ code, message, data }`
- 前端收到 401 时自动跳转登录页（`request.js` 响应拦截器）

---

### 第 3 站：登录认证（JWT）

打开 `AuthController.java`，搞清楚这个流程：

```
注册：POST /api/auth/register
  → 校验邮箱格式 + 密码长度
  → BCrypt 加密密码（不是明文存储！）
  → 存入 app_user 表，角色默认 USER

登录：POST /api/auth/login
  → 根据邮箱查用户
  → BCrypt 校验密码
  → 生成 JWT Token（包含 userId + role）
  → 返回 LoginVO（含 token + 用户信息）

后续请求：
  → 前端把 token 放在 Authorization: Bearer <token>
  → AuthInterceptor 拦截验证
  → 解析 token → 提取 userId/role → 存入 UserContext
  → Controller 从 UserContext 取当前用户信息
```

**关键技术点**：
- **JWT 无状态**：服务器不存 session，token 自带用户信息，过期时间 24 小时
- **BCrypt**：每次加密结果不同（有随机盐），但校验能匹配，比 MD5 安全得多
- **ThreadLocal**（`UserContext.java`）：一次请求内共享用户信息，请求结束自动清理
- **拦截器**（`AuthInterceptor.java`）：所有 `/api/**` 请求都拦截，只有 `/api/auth/**` 放行

---

### 第 4 站：理解分层设计

这是工程化思维的核心。同样是"账单"数据，为什么要三种 Java 类？

| 类型 | 用途 | 为什么不直接用 Entity |
|------|------|---------------------|
| **Entity** (`Bill.java`) | 对应数据库表 | 数据库字段 ≠ 业务需要的字段 |
| **DTO** (`BillCreateDTO.java`) | 接收前端传来的数据 | 前端不需要传 id、createTime |
| **VO** (`BillVO.java`) | 返回给前端的数据 | 前端不需要 passwordHash |

```
前端 → DTO → Service → Entity → 数据库
数据库 → Entity → Service → VO → 前端
```

> **为什么不用一个类通吃？** 安全。如果用 Entity 直接接收前端数据，恶意用户可能传入 `id=999` 覆盖别人的账单。DTO 只暴露允许修改的字段。

---

### 第 5 站：统计查询

打开 `StatisticsServiceImpl.java`，这里的 SQL 比增删改查复杂：

- **仪表盘**：`SUM(CASE WHEN type='income' THEN amount ELSE 0 END)` — 一条 SQL 算出收入/支出/结余
- **分类统计**：`GROUP BY category_id` — 按分类聚合
- **储蓄率**：`(收入 - 支出) / 收入 * 100` — 前端计算

**关键技术点**：
- MyBatis-Plus 的 `@Select` 注解可以直接写 SQL，复杂查询不用 XML
- 所有统计都带 `WHERE user_id = ?`，确保数据隔离

---

### 第 6 站：前端交互

等后端链路读通了再看前端，重点看三块：

#### 6.1 请求封装（`api/request.js`）
```
axios 实例 → 请求拦截器（加 token）→ 响应拦截器（401 跳登录）
```
这是所有 API 调用的基础，每个接口文件都 `import request from './request'`。

#### 6.2 根组件（`App.vue`）
```
未登录 → 显示 AuthPage
已登录 → Sidebar + FinanceDashboard + BackgroundParticles
```
登录状态存在 `localStorage`，刷新页面不丢失。

#### 6.3 主页面（`FinanceDashboard.vue`）
这是最复杂的组件，包含：
- 数据卡片（MetricCard）— 带硬币音效
- 账单列表（BillList）— 增删改查
- ECharts 图表（ChartPanel）— 分类饼图 + 趋势折线图
- 3D 小票（InteractiveReceipt）— 核心可视化

---

### 第 7 站：3D 小票可视化（InteractiveReceipt.vue）

这是项目最有技术含量的组件，用 Three.js 实现了一张**可拖拽的 3D 纸质小票**。

#### 技术架构

```
Three.js 场景
├── PlaneGeometry（细分网格）     ← 纸张几何体，44x76 个顶点
├── ClothSimulation              ← 布料物理引擎
│   ├── Verlet 积分              ★ 位置/速度隐式追踪
│   ├── 结构约束（水平+垂直）     ★ 保持网格不散架
│   ├── 剪切约束（对角线）        ★ 防止菱形变形
│   ├── 弯曲约束（隔一个）        ★ 纸张弯曲刚度
│   └── 顶部约束                 ★ 模拟被夹住悬挂
├── CanvasTexture                ← 小票内容（动态绘制财务数据）
├── Spring 系统                  ← 旋转/位移/缩放的弹簧动画
├── 阴影延迟 + 高光偏移          ← 多层视差运动
└── 设备性能自适应               ★ 根据 CPU/内存调整细分精度
```

#### 阅读顺序

1. **先看 `drawReceipt()`**：用 Canvas 2D API 画小票内容（文字、分隔线、条形码）
2. **再看 `ClothSimulation` 类**：Verlet 积分 → 约束求解 → 力传播
3. **最后看交互**：`onPointerDown/Move/Up` — 拖拽时如何施加力

**关键技术点**：
- **Verlet 积分**：不需要存速度，用 `当前位置 - 上一帧位置` 隐式计算，天然稳定
- **约束迭代 8 次**：每帧重复求解约束 8 次，让纸张更快回到稳定状态
- **`pinStrength`**：0 = 自由，1 = 完全固定，0.85 = 顶部强约束（不完全固定，允许微动）
- **性能分级**：低配设备用 20x36 细分，高配用 44x76

---

### 第 8 站：多角色权限系统

```
请求进来
  ↓
AuthInterceptor 拦截
  ↓
验证 JWT Token
  ↓
提取 userId + role → 存入 UserContext（ThreadLocal）
  ↓
检查 @RequireRole 注解
  ↓
有权限 → 放行到 Controller
无权限 → 返回 403
```

**三个角色的区别**：

| 角色 | 能做什么 | 不能做什么 |
|------|---------|-----------|
| ADMIN | 管理用户、查看所有数据 | — |
| USER | 管理自己的账单和分类 | 看别人的数据 |
| VIEWER | 只能查看 | 新增/修改/删除 |

**关键技术点**：
- `@RequireRole("ADMIN")` 是自定义注解，加在 Controller 方法上
- `UserContext` 用 `ThreadLocal` 存储，保证多线程下用户信息不串
- 数据隔离在 Service 层实现：所有查询都带 `WHERE user_id = ?`

---

## 五、关键技术点速查表

| 编号 | 技术点 | 在哪里 | 一句话解释 |
|------|--------|--------|-----------|
| K1 | JWT 认证 | `JwtUtil.java` + `AuthInterceptor.java` | 无状态 token，服务器不存 session |
| K2 | BCrypt 加密 | `AppUserServiceImpl.java` | 密码加盐哈希，不可逆 |
| K3 | ThreadLocal | `UserContext.java` | 一次请求内共享数据，线程隔离 |
| K4 | 自定义注解 | `RequireRole.java` | 声明式权限控制 |
| K5 | 统一返回格式 | `ApiResponse.java` | `{ code, message, data }` |
| K6 | DTO/VO 分离 | `dto/` + `vo/` | 入参和出参分开，安全 |
| K7 | MyBatis-Plus | `mapper/` | 不用写 SQL 就能 CRUD |
| K8 | 数据隔离 | `BillServiceImpl.java` | 所有查询带 userId |
| K9 | Verlet 积分 | `InteractiveReceipt.vue` | 布料物理的核心算法 |
| K10 | Spring 弹簧 | `InteractiveReceipt.vue` | 平滑动画的数学模型 |
| K11 | Canvas 纹理 | `InteractiveReceipt.vue` | 动态绘制小票内容 |
| K12 | Web Audio | `utils/sounds.js` | 浏览器原生音效合成 |
| K13 | axios 拦截器 | `api/request.js` | 统一加 token + 处理 401 |
| K14 | 环境变量占位 | `application.properties` | `${DB_URL:默认值}` 模式 |
| K15 | Redis 降级 | `RedisConfig.java` | 无 Redis 时自动用内存缓存 |

---

## 六、数据流全景图

```
┌─────────────┐     HTTP 请求      ┌──────────────────┐     SQL      ┌─────────┐
│             │  ──────────────→   │                  │  ─────────→  │         │
│  Vue 3 前端  │                    │  Spring Boot 后端  │              │  MySQL  │
│             │  ←──────────────   │                  │  ←─────────  │         │
└─────────────┘     JSON 响应      └──────────────────┘     结果集    └─────────┘
      │                                  │
      │                                  │
  ┌───┴───┐                         ┌───┴───┐
  │       │                         │       │
  │ Three │                         │  JWT  │
  │  .js  │                         │ Token │
  │       │                         │       │
  └───────┘                         └───────┘
  3D 小票渲染                      身份认证
```

---

## 七、遇到看不懂怎么办

**原则：遇到什么不懂就专项补什么，不要回头重新听课。**

| 看到什么 | 不懂什么 | 怎么补 |
|---------|---------|--------|
| `List<Bill>` | 泛型和集合 | 专项学 20 分钟 |
| `@Autowired` | Spring 依赖注入 | 搜索"Spring DI 入门" |
| `@Select("...")` | MyBatis 注解 | 搜"MyBatis-Plus 注解开发" |
| `ThreadLocal` | 线程本地变量 | 搜"Java ThreadLocal 原理" |
| `BCrypt` | 密码加密 | 搜"BCrypt vs MD5" |
| `Verlet 积分` | 物理模拟 | 搜"Verlet integration cloth" |
| `ref()` / `watch()` | Vue 3 响应式 | 搜"Vue 3 Composition API" |

> 补完马上回来继续读，每个知识点都有即时使用场景，不会学完就忘。

---

## 八、巩固方法：自己复现

读完一个功能，合上源码，自己从零写一遍。

### 复现计划

| 阶段 | 时间 | 复现内容 | 验证标准 |
|------|------|---------|---------|
| 第 1 周 | 2h | 新增 + 查询账单（后端） | Postman 能调通 |
| 第 2 周 | 3h | 登录注册 + token 验证 | 带 token 才能访问接口 |
| 第 3 周 | 4h | 分类管理 + 前后端联调 | 页面能增删改查 |
| 第 4 周 | 3h | 多角色权限控制 | ADMIN/USER/VIEWER 各有不同权限 |

> **写不出来的地方 = 没真正懂的地方。** 这是检验自己的唯一方式。

---

## 九、项目完整性与可行性检查

### 后端检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 数据库建表脚本 | ✅ | `schema.sql` 自动执行 + `sql/init.sql` 含示例数据 |
| 用户注册/登录 | ✅ | BCrypt 加密 + JWT token |
| 账单 CRUD | ✅ | 增删改查 + 数据隔离 |
| 分类管理 | ✅ | 全局分类 + 用户私有分类 |
| 统计接口 | ✅ | 仪表盘 + 分类统计 |
| 权限控制 | ✅ | 三角色 + 自定义注解 + 拦截器 |
| 全局异常处理 | ✅ | `GlobalExceptionHandler.java` |
| CORS 跨域 | ✅ | `CorsConfig.java` |
| Redis 可选 | ✅ | 默认内存缓存，配 Redis 自动切换 |
| 环境变量 | ✅ | 所有敏感配置支持 `${ENV:默认值}` |

### 前端检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 登录注册页 | ✅ | 邮箱登录 + 注册 |
| 主仪表盘 | ✅ | 数据卡片 + 账单列表 + 图表 |
| 3D 小票 | ✅ | Three.js 布料物理 + 拖拽交互 |
| 硬币音效 | ✅ | Web Audio API 合成 |
| 粒子动画 | ✅ | Canvas 粒子系统 |
| 国际化 | ✅ | 中/英切换 |
| 响应式 | ✅ | PC + 移动端适配 |
| API 代理 | ✅ | Vite 代理 `/api` → `localhost:8080` |
| 401 处理 | ✅ | 自动跳转登录页 |

### 上传前注意事项

1. **`.gitignore` 已配置**：`target/`、`node_modules/`、`dist/`、`.idea/`、`.env` 都已排除
2. **无硬编码密码**：数据库密码和 JWT 密钥都用环境变量
3. **无 Lombok 依赖**：代码全部手写 getter/setter，无需额外配置
4. **`finance-ui/dist/` 已存在**：如果不想上传构建产物，在 `.gitignore` 加上 `finance-ui/dist/`

---

## 十、一句话总结

> 现在就开始读，读一个功能就自己复现一个功能，遇到不懂的知识点专项补，不要等"学完"再开始。
