# 源码阅读指南

> 适用于：Java / Vue 初学者 · 个人财务管理系统
> 目标：从零看懂整个项目，每一步都知道自己在看什么、为什么这样写

---

## 一、项目全貌：30 秒理解这个系统在做什么

这是一个**前后端分离 + Docker 容器化部署**的个人财务管理 Web 应用：

```
用户注册/登录 → 记录收入和支出 → 按分类统计 → 3D 小票可视化展示
```

**技术架构一句话**：Vue 3 前端 → Nginx 反代 → Spring Boot 后端 → MySQL 存数据 → JWT 做身份认证 → Docker 一键部署

**三层容器部署架构**：

```
浏览器 → Nginx (:80)  ──/api/*──→  Spring Boot (:8080)  ──→  MySQL (:3306)
              │                                                      │
              └── /* → Vue SPA 静态文件                              Redis（可选缓存）
```

---

## 二、阅读前：你需要会什么

### 必须懂（否则看不懂）

| 知识点 | 最低要求 | 不懂怎么办 |
|--------|---------|-----------|
| Java 类和对象 | 知道什么是字段、方法、构造函数 | 专项补 30 分钟就够 |
| List / Map | 知道怎么存取数据 | 专项补 20 分钟 |
| 注解 | 看到 `@` 开头知道这是注解 | 搜索"Spring 注解入门" |
| SQL 基础 | SELECT / INSERT / UPDATE / DELETE | 能看懂就行 |
| HTTP 基础 | GET 和 POST 的区别 | 5 分钟就够 |
| JSON | 知道 `{"key": "value"}` 的结构 | 5 分钟 |

### 不需要先懂（项目里会学到）

- Spring Boot 怎么配置 → 看第三章
- MyBatis-Plus 怎么用 → 看第四章
- JWT 是什么 → 看第五章
- LambdaQueryWrapper 链式查询 → 看第五章统计部分
- Three.js 怎么画 3D → 看第七章
- Docker 容器化部署 → 看第九章

---

## 三、项目结构地图

先看整体目录，建立空间感：

```
my_project/
│
├── finance-ui/                        ← 前端（Vue 3 + Vite）
│   ├── src/
│   │   ├── api/                       ← 【关键】HTTP 请求封装
│   │   │   ├── request.js             ★ 统一 axios 实例 + JWT 注入 + 401 拦截
│   │   │   ├── auth.js                ← 登录注册接口
│   │   │   ├── bill.js                ← 账单 CRUD 接口
│   │   │   ├── category.js            ← 分类查询接口
│   │   │   └── statistics.js          ← 统计数据接口
│   │   │
│   │   ├── components/                ← UI 组件
│   │   │   ├── InteractiveReceipt.vue ★ 3D 小票（Three.js 布料物理）
│   │   │   ├── CoinParticles.vue      ← 硬币粒子动画（Canvas）
│   │   │   ├── BackgroundParticles.vue← 背景粒子
│   │   │   ├── MetricCard.vue         ← 数据卡片（带音效）
│   │   │   ├── BillList.vue           ← 账单列表
│   │   │   ├── ChartPanel.vue         ← ECharts 图表
│   │   │   ├── Sidebar.vue            ← 侧边栏导航
│   │   │   ├── AddTransactionModal.vue← 新增账单弹窗
│   │   │   └── ConfirmModal.vue       ← 确认删除弹窗
│   │   │
│   │   ├── views/                     ← 页面
│   │   │   ├── AuthPage.vue           ← 登录注册页
│   │   │   └── FinanceDashboard.vue   ★ 主仪表盘（核心页面）
│   │   │
│   │   ├── i18n/                      ← 国际化（中/英）
│   │   ├── utils/sounds.js            ← Web Audio 音效合成
│   │   ├── App.vue                    ★ 根组件（路由 + 登录状态）
│   │   ├── main.js                    ← 入口
│   │   ├── style.css                  ← 全局样式
│   │   └── tokens.css                 ← 设计令牌（CSS 变量）
│   │
│   ├── vite.config.js                 ★ Vite 配置（API 代理）
│   └── README.md                      ← 前端说明
│
├── src/main/java/com/example/personalfinance/   ← 后端（Spring Boot）
│   │
│   ├── controller/                    ← 接收 HTTP 请求
│   │   ├── AuthController.java        ★ 登录注册（入口）
│   │   ├── BillController.java        ★ 账单 CRUD（核心业务）
│   │   ├── CategoryController.java    ← 分类管理
│   │   ├── StatisticsController.java  ← 统计查询
│   │   └── AdminUserController.java   ← 管理员接口
│   │
│   ├── service/                       ← 业务逻辑
│   │   ├── impl/
│   │   │   ├── AppUserServiceImpl.java★ 用户业务（注册/登录/角色管理）
│   │   │   ├── BillServiceImpl.java   ← 账单业务实现
│   │   │   ├── CategoryServiceImpl.java
│   │   │   └── StatisticsServiceImpl.java ★ 统计查询（含缓存）
│   │   ├── IAppUserService.java       ← 接口定义
│   │   └── ...
│   │
│   ├── mapper/                        ← 数据库操作（MyBatis-Plus）
│   │   ├── BillMapper.java
│   │   ├── CategoryMapper.java
│   │   └── AppUserMapper.java
│   │
│   ├── entity/                        ← 数据库表对应的 Java 类
│   │   ├── Bill.java                  ★ 账单实体
│   │   ├── Category.java              ← 分类实体
│   │   └── AppUser.java               ← 用户实体
│   │
│   ├── dto/                           ← 前端 → 后端 的数据格式
│   │   ├── BillCreateDTO.java         ★ 新增账单的请求体
│   │   ├── BillUpdateDTO.java         ← 编辑账单的请求体
│   │   ├── LoginDTO.java              ← 登录请求体
│   │   ├── RegisterDTO.java           ← 注册请求体（含密码校验）
│   │   ├── BillQueryDTO.java          ← 账单查询条件
│   │   ├── CategoryCreateDTO.java     ← 分类创建
│   │   ├── CategoryQueryDTO.java      ← 分类查询条件
│   │   └── UserRoleUpdateDTO.java     ← 管理员修改角色
│   │
│   ├── vo/                            ← 后端 → 前端 的数据格式
│   │   ├── LoginVO.java               ★ 登录响应（含 token + role）
│   │   ├── BillVO.java                ★ 账单响应（Entity→VO 转换）
│   │   ├── DashboardVO.java           ← 仪表盘数据
│   │   └── CategoryStatVO.java        ← 分类统计
│   │
│   ├── interceptor/
│   │   └── AuthInterceptor.java       ★ JWT 认证拦截器（权限核心）
│   │
│   ├── annotation/
│   │   └── RequireRole.java           ★ 自定义角色注解
│   │
│   ├── common/
│   │   ├── ApiResponse.java           ← 统一返回格式
│   │   └── UserContext.java           ★ ThreadLocal 存当前用户
│   │
│   ├── config/
│   │   ├── CorsConfig.java            ← 跨域配置
│   │   ├── WebMvcConfig.java          ★ 拦截器注册 + BCrypt Bean
│   │   ├── RedisConfig.java           ← Redis 缓存配置（可选）
│   │   └── MyBatisPlusConfig.java     ← 分页插件配置
│   │
│   ├── exception/
│   │   └── GlobalExceptionHandler.java← 全局异常处理
│   │
│   ├── util/
│   │   └── JwtUtil.java               ★ JWT 工具类
│   │
│   └── PersonalFinanceApplication.java← 启动类
│
├── src/main/resources/
│   ├── application.properties         ★ 全部配置（环境变量占位）
│   └── schema.sql                     ← 建表脚本（自动执行）
│
├── sql/init.sql                       ← Docker 初始化脚本（含种子数据）
├── pom.xml                            ★ Maven 依赖配置
├── docker-compose.yml                 ★ 三容器编排
├── Dockerfile                         ← 后端镜像构建
├── Dockerfile.frontend                ← 前端镜像构建（Nginx）
├── nginx.conf                         ← Nginx SPA 路由 + API 反代
└── .env.example                       ← 环境变量模板
```

> **带 ★ 的文件是核心文件**，阅读时优先看这些。

---

## 四、推荐阅读路线

### 路线图

```
第1站：数据库 → 第2站：一条完整链路 → 第3站：登录认证
→ 第4站：分层设计 → 第5站：统计查询与缓存 → 第6站：前端交互
→ 第7站：3D 可视化 → 第8站：权限系统 → 第9站：Docker 部署
```

---

### 第 1 站：看数据库（建立地图）

打开 `src/main/resources/schema.sql`，做三件事：

1. **列出三张表**：`app_user`、`category`、`bill`
2. **看字段和类型**：注意 `amount` 用 `DECIMAL(10,2)` 不用 `DOUBLE`（为什么？浮点数有精度丢失，财务计算必须精确）
3. **看关联关系**：`bill.category_id` → `category.id`（外键约束）

```
app_user (1) ──── (N) bill        一个用户有多条账单
app_user (1) ──── (N) category    一个用户有多个私有分类
category  (1) ──── (N) bill       一个分类对应多条账单
```

**关键技术点**：
- `category.user_id = NULL` 表示全局分类（所有人可见），有值表示用户私有分类——这是用 NULL 做标记的经典设计模式
- `bill.user_id` 实现**数据隔离**——每个用户只能看到自己的账单
- `app_user.role` 默认 `USER`，支持 `ADMIN` / `USER` / `VIEWER` 三种角色
- 每个索引都对应一个高频查询场景：`idx_bill_consume_date` 对应按月份统计，`idx_bill_category_id` 对应按分类聚合

> **⚠️ 注意**：项目中有两个 SQL 文件。`sql/init.sql` 是 Docker 首次启动时执行的（通过 docker-entrypoint-initdb.d），`schema.sql` 是 Spring Boot 启动时执行。如果你用 Docker 部署，`init.sql` 先执行建表，`schema.sql` 的 `CREATE TABLE IF NOT EXISTS` 就不会再建表了。不用 Docker 的话只有 `schema.sql` 生效。

> 这是整个项目的骨架，看懂了后面不会迷路。

---

### 第 2 站：跟一条完整功能链路

从**新增账单**开始，按以下顺序读：

```
① 用户点击"新增"按钮
   ↓  文件：AddTransactionModal.vue
② axios 发出 POST /api/bills 请求
   ↓  文件：api/bill.js → api/request.js（自动加 JWT）
③ BillController.add() 接收请求
   ↓  文件：controller/BillController.java
④ 调用 billService.save(bill)
   ↓  文件：service/impl/BillServiceImpl.java（继承 MyBatis-Plus ServiceImpl）
⑤ MyBatis-Plus 自动生成 INSERT SQL
   ↓  基于 BillMapper（继承 BaseMapper<Bill>）
⑥ 写入 bill 表
   ↓  数据库
⑦ 返回 ApiResponse<BillVO>
   ↓  Controller 层用 BillVO.from(bill) 将 Entity 转为 VO
⑧ 触发硬币音效 + 粒子动画
   ↓  文件：utils/sounds.js + CoinParticles.vue
```

**每一步搞清楚**：数据叫什么名字？从哪里传到哪里？格式是什么？

**关键技术点**：
- `request.js` 的请求拦截器自动从 `localStorage` 取 token 加到 `Authorization` 头
- `BillController` 从 `UserContext.getUserId()` 取当前用户 ID，确保只能操作自己的数据
- `ApiResponse<T>` 统一包装返回格式：`{success: true/false, message: "...", data: ...}`
- 前端收到 401 时自动清除登录态并刷新页面（`request.js` 响应拦截器）
- Controller 返回 `BillVO` 而非 `Bill` Entity——这是分层设计的关键（见第 4 站）

---

### 第 3 站：登录认证（JWT）

打开 `AuthController.java` → `AppUserServiceImpl.java` → `JwtUtil.java`，搞清楚这个流程：

```
注册：POST /api/auth/register
  → 校验邮箱格式 + 密码长度（@Valid + RegisterDTO 注解）
  → BCrypt 加密密码（不是明文存储！）
  → 存入 app_user 表，角色默认 USER
  → 生成 JWT Token（包含 userId + email + role）
  → 返回 LoginVO（含 token + 用户信息）

登录：POST /api/auth/login
  → 根据邮箱查用户
  → BCrypt.matches(rawPassword, hashedPassword) 校验密码
  → 生成 JWT Token
  → 返回 LoginVO

后续请求：
  → 前端把 token 放在 Authorization: Bearer <token>
  → AuthInterceptor 拦截验证
  → jwtUtil.validateToken(token) → 验证签名 + 过期
  → jwtUtil.getUserId(token) → 提取 userId
  → jwtUtil.getRole(token) → 提取 role
  → UserContext.set(userId, role) → 存入 ThreadLocal
  → Controller 从 UserContext 取当前用户信息
```

**关键技术点**：
- **JWT 无状态**：服务器不存 session，token 自带用户信息，过期时间由 `jwt.expiration-ms` 配置（默认 24 小时）
- **BCrypt**：每次加密结果不同（有随机盐），但校验能匹配，比 MD5/SHA256 安全得多
- **ThreadLocal**（`UserContext.java`）：一次请求内共享用户信息，请求结束 `afterCompletion` 自动清理。为什么不用方法参数传递？因为每个 Controller 方法都要加参数太啰嗦，ThreadLocal 让任何代码都能随时取到当前用户。
- **拦截器**（`AuthInterceptor.java`）：所有 `/api/**` 请求都拦截，只有 `/api/auth/login` 和 `/api/auth/register` 放行（在 `WebMvcConfig.java` 中配置）
- **角色变更后旧 Token 失效**：Redis 中存储 `token_invalid:<userId>`，角色被修改后旧 Token 即使未过期也无法使用

---

### 第 4 站：理解分层设计

这是工程化思维的核心。同样是"账单"数据，为什么要三种 Java 类？

| 类型 | 示例 | 用途 | 为什么不混用 |
|------|------|------|-------------|
| **Entity** | `Bill.java` | 对应数据库表，和表字段一一映射 | 数据库字段 ≠ 业务需要的字段 |
| **DTO** | `BillCreateDTO.java` | 接收前端传来的数据 | 前端不需要传 id、createTime、userId |
| **VO** | `BillVO.java` | 返回给前端的数据 | 可以隐藏内部字段、添加计算字段 |

```
前端 → DTO → Service → Entity → 数据库
数据库 → Entity → Service → VO → 前端
```

> **为什么不用一个类通吃？** 安全 + 解耦。如果用 Entity 直接接收前端数据，恶意用户可能传入 `id=999` 覆盖别人的账单。DTO 只暴露允许修改的字段。VO 做 Entity → 响应 的转换，表结构变了不影响 API。

**💡 学习机会**：`BillVO.java` 是最近新增的。之前的代码直接返回 `Bill` Entity 给前端，虽然靠 `@JsonIgnore`（在 AppUser 上隐藏 passwordHash）暂时没出问题，但 Entity 的 `userId` 等内部字段也被暴露了。新增 `BillVO` 之后，通过 `BillVO.from(bill)` 静态工厂方法显式控制哪些字段可以返回给前端。这是项目演进中"发现设计不足 → 引入 VO 层"的真实案例。

**你还会看到**：
- `LoginDTO` → `AppUser`（Entity）→ `LoginVO`：登录场景的完整转换链
- `@JsonNaming(SnakeCaseStrategy.class)`：把 Java 的 camelCase（`consumeDate`）自动转为 JSON 的 snake_case（`consume_date`），前端直接用下划线字段名。这个注解在 Entity 和 VO 上都要加。

---

### 第 5 站：统计查询与缓存

打开 `StatisticsServiceImpl.java`，这里的查询比简单的 CRUD 复杂：

- **仪表盘**：查出当月所有账单 → 用 `sumByType()` 分别累加收入/支出金额 → 计算结余
- **分类统计**：查出所有支出账单 → 按 `category_id` 分组累加金额 → 关联分类名称
- **最近账单**：按 `consume_date` 倒序取前 5 条

**关键技术点**：

**① LambdaQueryWrapper 动态构建 SQL**：
```java
// 不用写一行 SQL！链式调用构建查询条件
LambdaQueryWrapper<Bill> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(Bill::getUserId, userId)           // WHERE user_id = ?
       .ge(Bill::getConsumeDate, startDate)   // AND consume_date >= ?
       .le(Bill::getConsumeDate, endDate)     // AND consume_date <= ?
       .orderByDesc(Bill::getConsumeDate);    // ORDER BY consume_date DESC
```
这是 MyBatis-Plus 的核心优势：用 Java 方法引用代替手写 SQL 字符串，编译期就能发现字段名写错。

**② Spring Cache 缓存**：
```java
@Cacheable(value = "dashboard", key = "#userId != null ? #userId : 'all'")
public DashboardVO getDashboard(Long userId) { ... }
```
- `@Cacheable` 注解表示"第一次调用后把结果缓存起来，下次直接返回缓存"
- `key` 中使用 `userId` 区分不同用户的缓存，避免 A 用户看到 B 用户的仪表盘数据
- 缓存加速了统计查询（无需每次都聚合计算），但要注意缓存失效策略

**③ Redis 可选降级**：
`@Autowired(required = false)` 表示 Redis 不是必需的：
```java
@Autowired(required = false)
private StringRedisTemplate redisTemplate;
```
如果 Redis 不可用，`redisTemplate` 为 null，系统自动跳过 Redis 操作，降级为内存缓存。这是一种**优雅降级**的生产级实践。

---

### 第 6 站：前端交互

等后端链路读通了再看前端，重点看三块：

#### 6.1 请求封装（`api/request.js`）
```
axios.create({baseURL: '/api'})
  → 请求拦截器：从 localStorage.pf_user 取 token → 附加 Authorization 头
  → 响应拦截器：检测 401 → 清除登录态 → 刷新页面
```
这是所有 API 调用的基础，每个接口文件都 `import request from './request'`。

#### 6.2 根组件（`App.vue`）
```
未登录 → 显示 AuthPage
已登录 → Sidebar + FinanceDashboard + BackgroundParticles
```
登录状态存在 `localStorage.pf_user`，刷新页面不丢失。项目**没有使用 vue-router**，用 `v-if` 切换登录/主页面——这对小项目足够，但如果你要扩展多页面，可以考虑引入 vue-router。

#### 6.3 主页面（`FinanceDashboard.vue`）
这是最复杂的组件，包含：
- KPI 指标条（月收入/支出/结余/笔数）
- 储蓄率进度条（带 25%/50%/75% 刻度线 + 等级评价）
- 筛选栏（全部/收入/支出）
- ECharts 图表面板（ChartPanel）— 旭日图 + 饼图
- 账单列表（BillList）— 分页 + 增删改
- 交互反馈 — 金币音效 + 粒子动画

---

### 第 7 站：3D 小票可视化（InteractiveReceipt.vue）

这是项目最有技术含量的组件，用 Three.js 实现了一张**可拖拽的 3D 纸质小票**。

#### 技术架构

```
Three.js 场景
├── PlaneGeometry（细分网格）     ← 纸张几何体，44×76 个顶点
├── ClothSimulation              ← 布料物理引擎
│   ├── Verlet 积分              ★ 位置/速度隐式追踪
│   ├── 结构约束（水平+垂直）     ★ 保持网格不散架
│   ├── 剪切约束（对角线）        ★ 防止菱形变形
│   ├── 弯曲约束（隔一个）        ★ 纸张弯曲刚度
│   └── 顶部固定约束             ★ 模拟被夹住悬挂
├── CanvasTexture                ← 小票内容（Canvas 2D 动态绘制财务数据）
├── Spring 系统                  ← 旋转/位移/缩放的弹性动画
├── 阴影延迟 + 高光偏移          ← 多层视差运动
└── 设备性能自适应               ★ 根据 CPU/内存调整细分精度
```

#### 阅读顺序

1. **先看 `drawReceipt()`**：用 Canvas 2D API 画小票内容（文字、分隔线、条形码）
2. **再看 `ClothSimulation` 类**：Verlet 积分 → 约束求解 → 力传播
3. **最后看交互**：`onPointerDown/Move/Up` — 拖拽时如何施加力

**关键技术点**：
- **Verlet 积分**：不需要存速度，用 `当前位置 - 上一帧位置` 隐式计算，天然稳定
- **约束迭代 8 次**：每帧重复求解约束 8 次，让纸张更快回到稳定状态
- **性能分级**：低配设备用 20×36 细分，高配用 44×76

---

### 第 8 站：多角色权限系统

```
请求进来
  ↓
AuthInterceptor 拦截
  ↓
验证 JWT Token → 提取 userId + role → 存入 UserContext（ThreadLocal）
  ↓
检查 @RequireRole 注解 → 对比当前用户角色
  ↓
有权限 → 放行到 Controller
无权限 → 返回 403 Forbidden
```

**三个角色的区别**：

| 角色 | 能做什么 | 不能做什么 |
|------|---------|-----------|
| ADMIN | 管理所有用户、查看所有数据、修改他人角色 | — |
| USER | 管理自己的账单和分类 | 看别人的数据、访问管理接口 |
| VIEWER | 只能查看自己的数据 | 新增/修改/删除任何数据 |

**关键技术点**：
- `@RequireRole({"ADMIN", "USER"})` 是自定义注解，加在 Controller 方法上
- `AuthInterceptor` 在 `preHandle` 阶段通过反射读取注解，比在每个方法里写 `if` 判权简洁得多
- 数据隔离分两层：**拦截器层**校验角色权限（能不能访问这个接口），**Service 层**校验数据所有权（能不能操作这条数据）
- ADMIN 修改用户角色后，被修改用户的旧 JWT **立即失效**（Redis 标记），防止被降级的用户继续用旧权限操作

---

### 第 9 站：Docker 部署架构

打开 `docker-compose.yml`，理解三个容器的协作：

```
┌───────────────────────────────────────────────────────────┐
│                    Docker Network                          │
│                                                           │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐  │
│  │   frontend   │   │   backend    │   │    mysql     │  │
│  │  Nginx + Vue │   │ Spring Boot  │   │   MySQL 8   │  │
│  │   Port 80    │   │  Port 8080  │   │  Port 3306  │  │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘  │
│         │                  │                  │          │
│         │  /api/* 代理     │   JDBC 连接      │          │
│         ├─────────────────►├─────────────────►│          │
│         │                  │                  │          │
│         │  静态文件 SPA    │                  │          │
│         │  (/* → index)   │                  │          │
└─────────┴──────────────────┴──────────────────┴──────────┘
```

**关键配置解读**：
- **depends_on + healthcheck**：后端等 MySQL 健康检查通过才启动，前端等后端就绪才启动
- **volumes**：`mysql_data:/var/lib/mysql` 持久化数据，容器删除数据不丢
- **init.sql 挂载**：`./sql/init.sql:/docker-entrypoint-initdb.d/01-init.sql` — MySQL 首次启动自动执行
- **Nginx try_files**：`try_files $uri /index.html` — SPA 路由核心，任何未知路径都返回 index.html

**启动命令**：
```bash
docker-compose up -d --build   # 构建并后台启动
docker-compose down            # 停止并删除容器
docker-compose logs -f backend # 查看后端日志
```

---

## 五、关键技术点速查表

| 编号 | 技术点 | 在哪里 | 一句话解释 |
|------|--------|--------|-----------|
| K1 | JWT 认证 | `JwtUtil.java` + `AuthInterceptor.java` | 无状态 token，服务器不存 session |
| K2 | BCrypt 加密 | `AppUserServiceImpl.java` | 密码加盐哈希，不可逆 |
| K3 | ThreadLocal | `UserContext.java` | 一次请求内共享数据，线程隔离 |
| K4 | 自定义注解 | `RequireRole.java` | 声明式权限控制，比 if-else 简洁 |
| K5 | 统一返回格式 | `ApiResponse.java` | `{success, message, data}` |
| K6 | DTO/VO 分离 | `dto/` + `vo/` | 入参和出参分开，安全 + 解耦 |
| K7 | MyBatis-Plus | `mapper/` + LambdaQueryWrapper | 不用写 SQL 就能 CRUD + 复杂查询 |
| K8 | 数据隔离 | `BillController.java` + Service 层 | 所有查询带 userId，ADMIN 除外 |
| K9 | Verlet 积分 | `InteractiveReceipt.vue` | 布料物理的核心算法 |
| K10 | Spring 弹簧 | `InteractiveReceipt.vue` | 平滑动画的数学模型 |
| K11 | Canvas 纹理 | `InteractiveReceipt.vue` | 动态绘制小票内容到 3D 材质 |
| K12 | Web Audio | `utils/sounds.js` | 浏览器原生音效合成 |
| K13 | axios 拦截器 | `api/request.js` | 统一加 token + 处理 401 |
| K14 | 环境变量占位 | `application.properties` | `${DB_URL:默认值}` 模式 |
| K15 | Redis 可选降级 | `@Autowired(required=false)` | 无 Redis 时自动跳过，不报错 |
| K16 | JSON 命名转换 | `@JsonNaming(SnakeCaseStrategy)` | Java camelCase ↔ JSON snake_case |
| K17 | Spring Cache | `@Cacheable` | 声明式缓存，按 userId 分 key |
| K18 | Docker 多容器 | `docker-compose.yml` | MySQL + Backend + Nginx 一键启动 |
| K19 | Nginx SPA 路由 | `try_files $uri /index.html` | 所有路径都返回 index.html |
| K20 | MyBatis Page.convert | `BillController.list()` | Page<Entity> → Page<VO> 一行转换 |

---

## 六、数据流全景图

```
┌─────────────┐     HTTP 请求      ┌──────────────────┐     SQL      ┌─────────┐
│             │  ──────────────→   │                  │  ─────────→  │         │
│  Vue 3 前端  │   Authorization:  │  Spring Boot 后端  │              │  MySQL  │
│  (Vite)     │    Bearer <JWT>   │                  │  ←─────────  │         │
│             │  ←──────────────   │                  │     结果集    │         │
└─────────────┘   {success,        └──────────────────┘              └─────────┘
      │           message, data}           │
      │                                    │
  ┌───┴───┐                           ┌───┴────┐
  │ Three │                           │  JWT   │      ┌─────────┐
  │  .js  │                           │ Token  │      │  Redis  │
  └───────┘                           └────────┘      │ (可选)  │
  3D 小票渲染                      身份认证 + 角色     └─────────┘
```

---

## 七、遇到看不懂怎么办

**原则：遇到什么不懂就专项补什么，不要回头重新听课。**

| 看到什么 | 不懂什么 | 怎么补 |
|---------|---------|--------|
| `List<Bill>` | 泛型和集合 | 专项学 20 分钟 |
| `@Autowired` | Spring 依赖注入 | 搜索"Spring DI 入门" |
| `@RestController` | Spring MVC 注解 | 搜"Spring Boot Controller" |
| `LambdaQueryWrapper` | MyBatis-Plus 链式查询 | 搜"MyBatis-Plus 条件构造器" |
| `ThreadLocal` | 线程本地变量 | 搜"Java ThreadLocal 原理" |
| `BCrypt` | 密码加密 | 搜"BCrypt vs MD5" |
| `@Cacheable` | Spring 声明式缓存 | 搜"Spring Cache 注解" |
| `docker-compose` | 容器编排 | 搜"Docker Compose 入门" |
| `Verlet 积分` | 物理模拟 | 搜"Verlet integration cloth" |
| `ref()` / `watch()` | Vue 3 响应式 | 搜"Vue 3 Composition API" |
| `@JsonNaming` | JSON 序列化 | 搜"Jackson PropertyNamingStrategy" |

> 补完马上回来继续读，每个知识点都有即时使用场景，不会学完就忘。

---

## 八、巩固方法：自己复现

读完一个功能，合上源码，自己从零写一遍。

### 复现计划

| 阶段 | 时间 | 复现内容 | 验证标准 |
|------|------|---------|---------|
| 第 1 周 | 2h | 新增 + 查询账单（后端） | Postman 能调通 |
| 第 2 周 | 3h | 登录注册 + token 验证 | 带 token 才能访问接口 |
| 第 3 周 | 4h | 分类管理 + 前后端联调 | 页面能增删改查 |
| 第 4 周 | 3h | 多角色权限控制 | ADMIN/USER/VIEWER 各有不同权限 |

> **写不出来的地方 = 没真正懂的地方。** 这是检验自己的唯一方式。

---

## 九、进阶学习路线（项目可扩展方向）

当你把现有代码读透、复现完成后，这些是你可以挑战的下一步。每个都是真实项目中的常见需求，按难度递增：

### 🟡 中阶扩展

| 方向 | 为什么值得做 | 涉及技术 |
|------|-------------|---------|
| **引入 vue-router** | 当前用 `v-if` 切换页面，多页面时必须用路由。这是 Vue 生态的基础设施 | vue-router 4, 路由守卫, 动态路由 |
| **添加 Swagger 文档** | 有了 API 文档，前后端联调更高效，也是团队协作的标配 | SpringDoc OpenAPI 3 |
| **单元测试** | 保证代码改不动坏，是职业开发者的基本功 | JUnit 5, Mockito, @WebMvcTest |
| **日志规范** | 当前只有零散 log，生产环境需要结构化日志 | SLF4J MDC, 请求追踪 ID |

### 🔴 高阶扩展

| 方向 | 为什么值得做 | 涉及技术 |
|------|-------------|---------|
| **预算管理** | 新增 `budget` 表 + 预算 vs 实际对比。这是财务系统的核心功能 | 数据库设计, 复杂 SQL, 前端对比图表 |
| **改用 Spring Security** | 当前手动写拦截器 + JWT，Spring Security 是更标准的做法，面试常问 | SecurityFilterChain, GrantedAuthority |
| **CI/CD 流水线** | 代码提交自动测试 + 构建镜像 + 部署 | GitHub Actions, Docker Hub |
| **WebSocket 实时推送** | 多人协同时实时通知账单变更 | STOMP, SockJS, 消息广播 |
| **数据导出** | 把账单导出为 Excel/PDF | Apache POI, JasperReports |

> **建议**：中阶扩展在复现完现项目后就可以尝试，高阶扩展等你对 Spring 生态更熟悉后再挑战。每做一个扩展都参考第 8 章的复现方法：先看懂 → 自己写 → 验证通过。

---

## 附录：项目完整性与可行性检查

### 后端检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 数据库建表脚本 | ✅ | `schema.sql` 自动执行 + `sql/init.sql` 含种子数据 |
| 用户注册/登录 | ✅ | BCrypt 加密 + JWT token |
| 账单 CRUD | ✅ | 增删改查 + 数据隔离 + Entity→VO 转换 |
| 分类管理 | ✅ | 全局分类 + 用户私有分类 |
| 统计接口 | ✅ | 仪表盘 + 分类统计 + Spring Cache 缓存 |
| 权限控制 | ✅ | 三角色 + 自定义注解 + 拦截器 + Redis Token 失效 |
| 全局异常处理 | ✅ | `GlobalExceptionHandler.java` |
| CORS 跨域 | ✅ | `CorsConfig.java` |
| Redis 可选 | ✅ | `@Autowired(required=false)` 优雅降级 |
| 环境变量 | ✅ | 所有敏感配置支持 `${ENV:默认值}` |
| Docker 部署 | ✅ | 三容器编排 + 健康检查 + 数据持久化 |

### 前端检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 登录注册页 | ✅ | 邮箱登录 + 注册 + JWT 存储 |
| 主仪表盘 | ✅ | KPI 卡片 + 账单列表 + ECharts 图表 |
| 3D 小票 | ✅ | Three.js 布料物理 + Canvas 纹理 + 拖拽交互 |
| 硬币音效 | ✅ | Web Audio API 合成 |
| 粒子动画 | ✅ | Canvas 粒子系统（背景 + 操作反馈） |
| 国际化 | ✅ | 中/英切换 |
| 响应式 | ✅ | PC + 移动端适配 |
| API 代理 | ✅ | Vite 开发代理 `/api` → `localhost:8080` |
| 401 处理 | ✅ | 自动跳转登录页 |

---

> 现在就开始读，读一个功能就自己复现一个功能，遇到不懂的知识点专项补，不要等"学完"再开始。
